#ifndef SEMANTICS_HH
#define SEMANTICS_HH

#include <iostream>

namespace semantics
{
    class Semantics
    {
    private:
        /* data */
    public:
        Semantics(/* args */);
        ~Semantics();
    };

} // namespace semantics

#endif // SEMANTICS_HH
